# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'GetBasisPageView' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for GetBasisPageView
  pod 'Alamofire', '~> 4.7'
  pod 'ReachabilitySwift'

  target 'GetBasisPageViewTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'GetBasisPageViewUITests' do
    # Pods for testing
  end

end
